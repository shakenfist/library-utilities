{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "gitAuthor": "shakenfist-bot <bot@shakenfist.com>",
  "dependencyDashboard": true,
  "assignees": [
    "mikalstill"
  ],
  "schedule": [
    "after 9pm"
  ],
  "pre-commit": {
    "description": "Renovate's pre-commit manager is opt-in. Without this the hook revisions in .pre-commit-config.yaml are the one dependency file nobody is watching.",
    "enabled": true
  },
  "packageRules": [
    {
      "description": "Minor, patch and digest updates are grouped for review rather than merged unattended. automerge stays off until this repository's CI has enough history to be trusted as a merge gate; it only started running with the consistency bootstrap.",
      "matchUpdateTypes": [
        "minor",
        "patch",
        "digest"
      ],
      "automerge": false
    }
  ],
  "minimumReleaseAge": "3 days",
  "rollbackPrs": true
}
